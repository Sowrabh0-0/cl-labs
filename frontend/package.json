{
  "name": "clahan-labs-vm-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node ./scripts/serve-dist.mjs",
    "build": "tsc -p tsconfig.json && node ./scripts/copy-assets.mjs",
    "preview": "node ./scripts/serve-dist.mjs"
  },
  "dependencies": {
    "typescript": "^5.6.3"
  },
  "devDependencies": {}
}
